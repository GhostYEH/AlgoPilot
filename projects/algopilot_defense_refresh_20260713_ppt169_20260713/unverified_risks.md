# 尚未核验或存在风险的事实列表

1. **归档 188 与当前 190 不一致**：`03_测试说明书.md` 记录 2026-07-12 为 188 passed；2026-07-13 在 commit `dc3e1503…` 对当前代码重跑为 190 passed。Slide 17 按用户指定保留 188 归档快照；其对应 commit 未记录，标“待补充”。
2. **20 个已实现节点不是代码内可直接统计字段**：`AGENT_REGISTRY` 可稳定统计 22 个节点，但没有 `status` 属性；20/2 来自开发说明书。若答辩前需要更强证据，应给注册项增加显式实现状态或生成校验清单。
3. **旧动态数字已过期**：旧文档的 70 个 Vue 组件、30 余页面、63 条 API、7 张表与当前仓库统计分别为 73、23 个视图文件、67 个路由装饰器、8 张 ORM 表。最终 PPT 不应继续引用旧值。
4. **API 67 的统计口径**：统计源代码中的 FastAPI HTTP 路由装饰器，不是启动后运行时路由实例；打包模式 SPA fallback 也包含在装饰器统计中。
5. **页面数量口径变化**：当前可稳定复现的是 23 个 `views/**/*.vue` 文件和 24 个命名路由，无法稳定复现“30 余页面”，因此已删除旧表述。
6. **C++ Trace 超时旧口径错误**：当前 `CPP_TRACE_SUBPROCESS_CAP_S=30.0`，不得继续写“C++ Trace 3 秒”；OJ 题目默认时限仍为 3000ms，两者需分开解释。
7. **截图同版本风险**：四张原始截图的文件修改时间跨 2026-07-12 与 2026-07-13，仓库没有内嵌截图构建 commit 元数据。交付文件已冻结 SHA-256，后续禁止替换；但“由同一 commit 构建”仍无法独立核验。
8. **学习路径截图被放大**：原始 1084×610 有效裁切被放大到 1680×945，细节清晰度需在正式页面制作时检查；不得通过 AI 锐化或重绘补造细节。
9. **生产化能力未完成**：容器级代码沙箱、HTTPS、监控、备份、高并发和独立判题队列属于后续升级项，不得写成现有能力。
10. **测试耗时不可跨机器比较**：54.05 秒、2.43 秒、当前 116.07 秒均受机器和缓存影响，只能作为快照备注，不得作为性能指标。
# 制作阶段新增风险

- 第 20 页独立 AI 结尾背景生成失败：host-native 多次网络错误，备用 SiliconFlow 后端返回身份校验 403。当前 closing_path_hero.png 复用已生成的封面蓝图资产，不包含新增文字、界面、数据或技术结构；后续可直接替换。
