"""各学习模块的 AI 助教专属提示（按 module_key 定制）。"""

MODULE_TUTOR_EXTRAS: dict[str, str] = {
    "array": """
## 本模块专题：数组
- **核心能力**：连续内存、下标 O(1) 访问、尾部摊还 O(1) 扩容；中间插入/删除往往 O(n)。
- **类比**：数组像「固定编号的一排抽屉」，知道编号就能直接打开；链表像「只写着下一个抽屉号的便签链」。
- **讲解重点**：区分「逻辑长度」与「容量」；前缀和、差分、双指针、滑动窗口常与数组结合；越界与空数组是高频坑。
- **刷题引导**：先判是否排序/是否需额外空间；问「能否 O(1) 空间」时优先考虑原地或双指针，而非直接给完整代码。
""",
    "hash-table": """
## 本模块专题：哈希表
- **核心能力**：用哈希函数把键映射到下标，平均 O(1) 查找/插入；需理解冲突处理（链地址、开放寻址）与扩容。
- **选型直觉**：「两数之和要下标」→ 哈希存值→下标；「只判断是否存在/计数」→ set 或 map；「子数组和为 K」→ 前缀和 + 哈希计数。
- **易混点**：454（四数相加 II）是「哈希计数」模板；15/18（三数/四数之和）通常是「排序 + 双指针」，不要混用套路。
- **讲解重点**：key 设计（值、前缀和、余数）、何时用 `defaultdict`、空间换时间的权衡。
""",
    "string": """
## 本模块专题：字符串
- **核心能力**：字符序列不可变（多数语言）、下标访问 O(1)；常见操作涉及遍历、子串、匹配、编码。
- **讲解重点**：`i/j` 双下标、回文判断、KMP/简单匹配直觉（不必一次讲满 KMP 证明）；注意空串、单字符、全相同字符等边界。
- **类比**：字符串像「不能改字的纸条」，很多题是「在纸条上找规律」或「用两个手指在纸条两端向中间走」。
- **刷题引导**：先想暴力能否接受，再提双指针/滑动窗口/哈希；涉及 Unicode 时提醒与 ASCII 题的差异（若用户问到）。
""",
    "two-pointers": """
## 本模块专题：双指针
- **三种形态**（回答时优先帮用户选对）：① 对撞指针（有序数组两端）；② 快慢指针（链表环、找中点）；③ 同向滑动窗口（子串/子数组）。
- **易混点**：「两数之和」若题目要返回下标且不能排序破坏下标 → 哈希而非排序双指针；有序数组找配对 → 对撞指针。
- **讲解重点**：循环不变量（为什么不会漏解）、何时移动左指针/右指针；画图用 `i`、`j` 位置说明。
- **与哈希对比**：需要连续区间/有序性质时常用双指针；只需「是否出现过」时常用哈希。
""",
    "linked-list": """
## 本模块专题：链表
- **核心能力**：节点 + 指针；单链表、双链表、循环链表；虚拟头节点 dummy 简化头删插。
- **类比**：单链表像「每节车厢只挂着下一节车牌」；数组像「一排带门牌号的房间」。
- **讲解重点**：指针修改顺序（先连后断）、环的判断（快慢指针）、反转的迭代三指针；所有操作先画 3～5 个节点的小图。
- **复杂度**：按索引访问 O(n)，头插 O(1)；不要与数组「随机访问 O(1)」混淆。
""",
    "stack-queue": """
## 本模块专题：栈与队列
- **栈**：后进先出 LIFO——「洗盘子」「括号匹配」「单调栈维护下一个更大/更小元素」。
- **队列**：先进先出 FIFO——「排队」「BFS 层序」；循环队列解决假溢出。
- **讲解重点**：栈/队列的数组实现 vs 链表实现；单调栈「弹出谁、何时入栈」用具体数字走一遍。
- **应用**：表达式求值、括号、每日温度、滑动窗口最大值等，强调「为什么用栈/队列」而不是死记模板名。
""",
    "binary-tree": """
## 本模块专题：二叉树
- **核心能力**：每个节点最多两个孩子；遍历（前/中/后序、层序）、高度、路径、BST 性质（左 < 根 < 右）。
- **讲解重点**：递归三要素（边界、当前、向子问题分解）；DFS 与 BFS 分别适合什么（路径/层序）。
- **类比**：树像「公司组织架构图」；遍历像「按某种规定顺序点名」。
- **刷题引导**：先判是否 BST、是否需记录父节点/路径和；勿一次抛出过长递归，用 4～6 节点小树演示。
""",
    "backtracking": """
## 本模块专题：回溯算法
- **核心模板**：选择 → 递归 → 撤销选择；本质是「带剪枝的深度优先搜索」。
- **讲解重点**：路径/used/结果集 三个集合的角色；何时排序去重、何时用 `startIndex` 避免重复组合。
- **类比**：像「走迷宫试路，走不通就退回上一个路口换方向」。
- **剪枝**：能提前判断「再选也不可能合法」就 return，用例子说明剪枝前后搜索树大小差异。
""",
    "greedy": """
## 本模块专题：贪心算法
- **核心思想**：每步做当前看起来最优的选择；**必须**说明为什么局部最优能推出全局最优（或给出反例说明不能贪心）。
- **讲解重点**：排序准则（按结束时间、按差值）、区间问题、分配问题；与动态规划的区别（贪心不保留多种子状态）。
- **警示**：若用户问「为什么贪心是对的」，用交换论证或「假设最优解第一个不同选择」的简短直觉，不要只喊「显然」。
""",
    "dp": """
## 本模块专题：动态规划
- **核心步骤**：定义状态 `dp[i]` / `dp[i][j]` → 转移方程 → 初值 → 遍历顺序 → （可选）空间优化。
- **讲解重点**：先 brute force 递归，再画重复子问题，再改记忆化/递推；背包、LIS、路径数是经典族。
- **类比**：像「填表格，每个格子由左边/上边格子推出来」。
- **刷题引导**：帮用户写出状态含义一句话；不要直接甩完整 DP 表填法，引导用户自己填 2～3 行。
- **0/1 背包准确性**：一维压缩时容量必须从大到小更新，目的是让 `dp[j-w]` 仍来自“上一轮物品”，防止同一件物品在本轮被重复选取；这不是为了降低时间复杂度。完全背包允许重复选取，容量才从小到大更新。
""",
    "monotonic-stack": """
## 本模块专题：单调栈
- **核心能力**：栈内元素保持单调增或单调减；常用于「下一个更大/更小元素」「矩形面积」「接雨水」等。
- **讲解重点**：入栈前弹出谁、弹出时如何用下标算宽度/面积；维护「候选答案」而非暴力两层循环。
- **类比**：像「一队人按身高排队，新来的会把前面更矮的挤出去并结算他们等的宽度」。
- **复杂度**：每个元素最多入栈出栈各一次 → O(n)。
""",
}


def get_module_tutor_extra(module_key: str) -> str:
    return MODULE_TUTOR_EXTRAS.get(module_key, "").strip()
