# 第 2 章 链表与指针模型

## 核心问题
- 如何用节点和指针表达非连续线性结构。
- 如何在插入、删除、反转时保持链路不断裂。
- 如何用虚拟头结点统一头部边界。

## 知识结构
- 抽象模型：节点包含 `value` 与 `next`。
- 常用技巧：虚拟头结点、快慢指针、三指针反转、递归合并。
- 复杂度：按节点遍历通常 O(n)，额外空间取决于递归或迭代实现。

## 典型任务
- 反转链表、合并有序链表、删除倒数第 N 个节点。
- Trace 重点：`prev/curr/next` 在每一步的指向。
