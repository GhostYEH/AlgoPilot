[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: 标记较慢的测试（真实代码执行/编译）
    needs_cpp: 需要 g++ 工具链的测试
    needs_gdb: 需要 gdb 的测试
filterwarnings =
    ignore::DeprecationWarning