# AlgoPilot Frontend

Vue 3 + TypeScript + Vite + Element Plus 前端。

## 启动

```powershell
npm install
npm run dev
```

## 验证

```powershell
npm run typecheck
npm run build
```

通过 `VITE_API_BASE_URL` 配置后端地址。登录后的学习进度会合并云端记录，并在小节完成、游戏通关等行为发生时同步到当前账号。
